let CONST = require("js/const.js");
let Card = require("js/card.js");
let Deck = require("js/deck.js");

var app = new Vue({
    el: '#app',
    data: {
        deck: new Deck(),
    },

    methods: {
    }
});